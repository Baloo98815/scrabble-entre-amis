# Copier vers infra/.env et remplir avant de lancer infra/deploy.sh
DOMAIN=scrabble.exemple.fr
LETSENCRYPT_EMAIL=toi@exemple.fr

POSTGRES_USER=scrabble
POSTGRES_PASSWORD=change-moi
POSTGRES_DB=scrabble

# Générer une valeur aléatoire solide, par ex: openssl rand -base64 48
JWT_SECRET=change-moi-aussi
